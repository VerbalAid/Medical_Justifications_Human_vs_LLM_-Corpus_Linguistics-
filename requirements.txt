# Corpus pipeline (Python 3.10–3.13 recommended; 3.14 may be uneven for some scientific stacks)
datasets>=2.14.0
huggingface_hub>=0.20.0
ollama>=0.3.0
nltk>=3.8
scikit-learn>=1.3.0
matplotlib>=3.7.0
networkx>=3.0
numpy>=1.24.0
pandas>=2.0.0

# Optional — NER + SNOMED depth (snomed_ner_depth.py). MUST use python3.12 for the venv:
#   rm -rf .venv && python3.12 -m venv .venv && source .venv/bin/activate
# (On Fedora 43, `python3 -m venv .venv` is often 3.14 → spaCy builds from source → numpy/Python.h fails.)
#   python -m pip install -U pip scispacy
#   python -m pip install https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.4/en_core_sci_lg-0.5.4.tar.gz
# Bucket: ai2-s2-scispacy (not ai2-s3-scispacy).  sudo dnf install python3.12-devel  if needed.
